# disabled — conflicts with static deployment
name: Deploy Jekyll with GitHub Pages dependencies preinstalled
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"

